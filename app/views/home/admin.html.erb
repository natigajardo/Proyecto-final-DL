<% content_for :header do %>  
  <div class="panel-admin">
    <h2 class="text-center py-3 title-panel-admin">Panel del administrador</h2>
  </div>
<% end %>

<hr/>

<div class="container mt-2">
    <!-- Nav vertical panel de admin -->
    <ul class="nav nav-tabs navbar-dark text-center" id="myTab" role="tablist">

      <li class="nav-item" role="presentation">
        <a class="nav-link active text-panel-admin" id="dashboard-tab" data-toggle="tab" href="#dashboard" role="tab" aria-controls="dashboard" aria-selected="false">Dashboard</a>
      </li>
      <li class="nav-item" role="presentation">
        <a class="nav-link text-panel-admin" id="orders-tab" data-toggle="tab" href="#orders" role="tab" aria-controls="orders" aria-selected="false">Ordenes</a>
      </li>
      <li class="nav-item" role="presentation">
        <a class="nav-link text-panel-admin" id="admin-tab" data-toggle="tab" href="#admin" role="tab" aria-controls="admin" aria-selected="false">Administradores</a>
      </li>
      <li class="nav-item" role="presentation">
        <a class="nav-link text-panel-admin" id="users-tab" data-toggle="tab" href="#users" role="tab" aria-controls="users" aria-selected="false">Usuarios</a>
      </li>
      <li class="nav-item" role="presentation">
        <a class="nav-link text-panel-admin" id="products-tab" data-toggle="tab" href="#products" role="tab" aria-controls="products" aria-selected="false">Productos</a>
      </li>
    </ul>

    <!-- Content del admin -->
    <div class="tab-content page-content table-responsive" id="myTabContent">

      <div class="tab-pane fade show active ml-1" id="dashboard" role="tabpanel" aria-labelledby="dashboard-tab">
        <%= render 'home/dashboard' %>
      </div>

      <div class="tab-pane fade" id="orders" role="tabpanel" aria-labelledby="orders-tab">
        <%= render 'home/orderstable' %>
      </div>

      <div class="tab-pane fade" id="admin" role="tabpanel" aria-labelledby="admin-tab">
        <%= render 'home/adminstable' %>
      </div>

      <div class="tab-pane fade" id="users" role="tabpanel" aria-labelledby="users-tab">
      <%= render 'home/userstable' %>
      </div>

      <div class="tab-pane fade" id="products" role="tabpanel" aria-labelledby="products-tab">
      <%= render 'home/producttable'%>
      </div>

    </div>

</div>






